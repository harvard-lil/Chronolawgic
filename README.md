# Chronolawgic
A legal timeline tool

Open two shell tabs, one for client, one for server.
In client tab:
```
$ cd client
$ npm install
$ npm run
```

In server tab
```
$ cd server
$ npm install
$ npm run
``` 